apiVersion: serving.knative.dev/v1
kind: Service
metadata:
  name: 'nextjs-deploy-dev'
spec:
  template:
    metadata:
      annotations:
        autoscaling.knative.dev/maxScale: '1'
    spec:
      containers:
      - name: 'app'
        image: 'app'
        env:
          - name: 'TARGET'
            value: 'Staging'
  traffic:
  - revisionName: dev-tag-new
    tag: new-tag-dev
    percent: 90
  - revisionName: dev-tag-old
    percent: 10